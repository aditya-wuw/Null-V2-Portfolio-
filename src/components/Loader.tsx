import { MoonLoader } from 'react-spinners'

const Loader = () => {
  return (
    <div className="flex justify-center p-3">
      <MoonLoader color="#004fff" size={30} />
    </div>
  )
}

export default Loader
